<div id="myModal" class="modal-container">
  <div class="modal">
    <div class="modal-head">
      <div class="modal-title">Add Leave</div>
      <div class="modal-close">
        <%- include("../partials/icons/close-svg"); -%>
      </div>
    </div>
    <div class="modal-content">
      <div class="container">
        <form action="/options/add-leaveday" method="post">
          <label for="name">
            <div class="">
              <select class="select" name="userId">
                <option disabled selected>Select Employee</option>
                <% console.log(selectEmployee) %> <% selectEmployee.map(el =>
                {%>
                <option value="<%= el.id %>"><%= el.user_name %></option>

                <% }) %>
              </select>
              <p class="error userId-error"></p>
              
              </div>
              <div class="">
                <select class="select" name="typeId">
                <option disabled selected>Select Type</option>
                <% leaveTypeList.map(el =>
                {%>
                <option value="<%= el.id %>"><%= el.name %></option>

                <% }) %>
              </select>
              <p class="error typeId-error"></p>
              </div>
              </label>
          <label for="projectDetails">
            Select Start Date
            <div class="">
              <input type="date" name="start" />
              <p class="error start-error"></p>
            </div>
          </label>
          <label for="projectDetails">
            Select End Date
            <div class="">
              <input type="date" name="end" />
              <p class="error end-error"></p>
            </div>
          </label>
          <div class="">
            <button class="submit-btn">Save</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
